# DELETED in phase 1.1: see docs/mantle-owns-it.md
#
# This file was `pin_leaves!`: walking a structure and pinning every array it
# reaches, so a submission held them. What a submission holds is core's
# (`graph/submission.jl`), and phase 2.2 gives it the list.
